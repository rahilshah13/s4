version: '3.8'

services:
  s4_gateway:
    build:
      context: .
      dockerfile_inline: |
        # syntax=docker/dockerfile:1.6
        FROM alpine:3.19 AS builder
        RUN apk add --no-cache gcc musl-dev sqlite-dev make git libev-dev
        WORKDIR /app
        COPY s4.h .
        COPY s4.c .
        RUN gcc -O2 s4.c -lpthread -lsqlite3 -o s4_daemon
        FROM alpine:3.19
        RUN apk add --no-cache sqlite-libs libev
        WORKDIR /app
        COPY --from=builder /app/s4_daemon /app/s4_daemon
        EXPOSE 8080
        CMD ["/app/s4_daemon"]
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  s4_ui:
    build:
      context: .
      dockerfile_inline: |
        FROM alpine:3.20

        RUN apk add --no-cache python3 py3-pip py3-requests git nodejs npm
        RUN WITH_EXTENSION=0 python3 -m pip install --no-cache-dir --break-system-packages mwparserfromhell

        COPY . /S4_UI_SRC
        WORKDIR /S4_UI_SRC

        RUN rm -rf .output .vinxi node_modules dist

        RUN cat <<'EOF' > package.json
        {
          "name": "s4-provider-ui",
          "type": "module",
          "scripts": {
            "dev": "vinxi dev",
            "build": "vinxi build",
            "start": "vinxi start"
          },
          "dependencies": {
            "@solidjs/meta": "^0.29.4",
            "@solidjs/router": "^0.15.3",
            "@solidjs/start": "^1.0.10",
            "solid-js": "^1.9.3",
            "vinxi": "^0.5.7"
          }
        }
        EOF

        RUN cat <<'EOF' > app.config.js
        import { defineConfig } from "@solidjs/start/config";

        export default defineConfig({
          server: {
            compressPublicAssets: false,
            experimental: {
              asyncContext: true
            }
          }
        });
        EOF

        RUN mkdir -p src/routes

        RUN if [ -f index.jsx ]; then cp index.jsx src/routes/index.jsx; fi

        RUN cat <<'EOF' > src/entry-client.jsx
        import { mount, StartClient } from "@solidjs/start/client";

        mount(() => <StartClient />, document.getElementById("app"));
        EOF

        RUN cat <<'EOF' > src/entry-server.jsx
        import { createHandler, StartServer } from "@solidjs/start/server";

        export default createHandler(() => (
          <StartServer
            document={({ assets, children, scripts }) => (
              <html lang="en">
                <head>
                  <meta charset="utf-8" />
                  <meta name="viewport" content="width=device-width, initial-scale=1" />
                  <title>S4 Service Provider Console</title>
                  {assets}
                </head>
                <body style="margin: 0; background-color: #111827; color: #f3f4f6; font-family: system-ui, -apple-system, sans-serif;">
                  <div id="app">{children}</div>
                  {scripts}
                </body>
              </html>
            )}
          />
        ));
        EOF

        RUN cat <<'EOF' > src/app.jsx
        import { Router } from "@solidjs/router";
        import { FileRoutes } from "@solidjs/start/router";
        import { Suspense } from "solid-js";

        export default function App() {
          return (
            <Router
              root={props => (
                <Suspense>{props.children}</Suspense>
              )}
            >
              <FileRoutes />
            </Router>
          );
        }
        EOF

        RUN npm install
        RUN npm run build

        # Ensure static assets are linked where Nitro looks for them at runtime
        RUN mkdir -p .output/server/chunks/public && cp -r .output/public/* .output/server/chunks/public/

        EXPOSE 3000
        CMD ["npm", "run", "start"]
    ports:
      - "3000:3000"
    depends_on:
      - s4_gateway